# 모든 텍스트 파일은 자동으로 줄바꿈 정리
* text=auto

# CSV 파일은 항상 LF 줄바꿈으로 저장 (협업시 권장)
*.csv text eol=lf

# Python, JS, HTML 등 일반 코드 파일도 LF로 맞추는 경우가 많음
*.py text eol=lf
*.js text eol=lf
*.html text eol=lf
*.css text eol=lf